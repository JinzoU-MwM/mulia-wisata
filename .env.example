# Local SQLite via libSQL
DATABASE_URL="file:./local.db"

# Better Auth — generate a strong secret (min 32 chars) for production
BETTER_AUTH_SECRET="change-me-to-a-long-random-string"
BETTER_AUTH_URL="http://localhost:3000"

# Public site config
NEXT_PUBLIC_WHATSAPP_NUMBER="6281234567890"

# Where uploaded images are stored on disk (defaults to ./uploads).
# On a VPS, point this at a persistent volume so images survive redeploys.
# UPLOAD_DIR="/var/www/mulia-indah/uploads"

# Seed admin credentials (used by `npm run db:setup`)
ADMIN_EMAIL="admin@muhiyahglobaltravel.id"
ADMIN_PASSWORD="MuhiyahGlobal#2026"
ADMIN_NAME="H. Faisal R."
